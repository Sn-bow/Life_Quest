# 첫 실행 흐름 감사 — 2026-09-14 KST

관찰 대상: bdc7801의 기존 UI, Flutter 3.47 아이콘 호환 수정 후 QA preview. 412 × 892 브라우저 화면. Android 실기기 감사와 구별한다. `qa_artifacts/rebirth/before-today.png`, `reference-lifeup.png`, `reference-arise-gallery.png`에 원본 화면 저장.

1. 첫 화면의 390px가 설명형 요약 패널이다. 실제 퀘스트 완료 버튼이 첫 viewport에 없다. 오늘의 3개 행동을 먼저 노출한다.
2. 하단 7개 목적지는 좁고 텍스트가 작다. 오늘/퀘스트/던전/성장 4개로 묶고 장비·스킬·상점·업적은 성장에 배치한다.
3. 완료 0개 상태에서도 첫 턴 드로우, 마법 카드, 악마의 도박 같은 용어가 나온다. 행동 전에는 시간·구체적인 할 일·보상을 설명한다. 게임 용어는 던전에서 안내한다.
4. 퀘스트 목록에도 동일한 큰 설명 패널이 반복된다. 목록의 첫 화면에서 바로 체크할 수 있게 줄인다.
5. 모든 패널에 비슷한 색·굵기·둥근 테두리를 사용해 시선의 우선순위가 불분명하다. 강한 상태 요약 하나와 간결한 행동 행을 사용한다.
6. 다음날 미완료로 HP·레벨·XP를 잃는 코드가 있다. 쉬고 돌아온 사용자가 누적 성장을 잃지 않게 변경하고 회복용 작은 행동을 제안한다.

참고 적용: LifeUp Play 실제 목록의 한 행/한 행동과 완료 컨트롤, Finch의 작은 행동 및 부드러운 복귀 유도, ARISE 공식 갤러리의 짙은 배경과 보라·청록 대비, Material의 3~5개 핵심 내비게이션. 웹툰 캐릭터/로고/갤러리 이미지 자체를 제품에 복사하지 않는다.

기능 판단은 이 앱의 실제 화면/코드에 기반하며 외부 제품의 리텐션 효과를 실증한 것으로 보지 않는다.

## 던전 첫 탐험 관찰 — 2026-09-14

관찰: 2.0 브라우저 412 × 892, 기기 전용 합성 QA 프로필. 실제 저장한 화면을 열어 확인했다. 범위는 던전 탭 → 1구역 지도 → 안내 → 카드 2회 사용 → 승리 → 보상 카드 선택 → 지도 1/12 복귀다. 전투 HP 80, 골드 50→61, 덱 10→11로 보상이 적용되었다.

| 단계 / 화면 파일 (`qa_artifacts/rebirth/`) | 관찰 | 수정 우선순위 |
|---|---|---|
| `dungeon-before-entry.png` | 첫 구역 진입이 약 500px 높이의 시즌/0 보정 카드 아래에 있다. 시즌 D-198은 현재 제공 콘텐츠와 종료 의미가 불명확하다. | 첫 탐험 CTA를 앞에 두고 보정 상세를 접는다. 의미 없는 시즌 마감 표현을 없앤다. |
| `dungeon-before-map.png` | 선택 가능한 지도 노드가 접근성 버튼으로 노출되지 않는다. 범례가 잘리고, 시작 노드 안내가 없다. | 상태·행·경로 이름, 키보드 조작, 선택 안내를 제공한다. |
| `dungeon-before-tutorial.png` | 안내 아래 카드·적·턴 종료 버튼이 접근성 트리에 남는다. 한국어에도 Block/End Turn 혼용이다. | 안내 중 배경 조작/포커스를 막고 4개 언어로 번역한다. |
| `dungeon-before-battle.png` | 공격 1회가 실제로 6 피해와 1 에너지를 소모한다. 뒤로 버튼은 이름이 없다. | 전투 나가기 이름과 적절한 터치 크기를 제공한다. |
| `dungeon-before-victory.png` | 3개 카드 설명이 좁은 한 줄의 약 100px 카드에 갇힌다. 보상 선택 시 실제 덱에 추가되고 지도에 복귀한다. | 보상은 기존 그림을 곁들인 읽기 가능한 세로 선택 목록으로 바꾼다. |

참고 원칙은 앞서 조사한 LifeUp의 한 행/한 행동과 기존 게임 아트의 유지, Material의 버튼/대화상자 패턴이다. 기존 배경·몬스터·카드 그림은 재사용한다. 이 관찰은 Android TalkBack 전반이나 게임 밸런스 전체를 검증한 것으로 보지 않는다.

## 수정 후 확인

- 실제 브라우저에서 첫 탐험 CTA → 이름 있는 지도 버튼 → 전투 → 도움말 열기/닫기 → 공격·드로우·방어·턴 종료 → 승리 → 카드 선택 → 지도 복귀를 확인했다. 보상은 골드 50→62, 덱 10→11, 지도 1/12로 반영됐다.
- 안내와 보상 중 배경의 전투 버튼은 접근성 트리에서 제외됐고 안내 버튼에 포커스가 이동했다. 전투 도중 도움말을 다시 열 수 있다.
- 4언어/320px/200% 글자에서 첫 탐험·지도·보상 선택 테스트 8개 통과. 영어 큰 글자에서 지도 영역 높이가 음수가 되던 실제 문제를 전체 페이지 스크롤과 높이 하한으로 수정했다.
- 첫 전투 완료 후 선택하지 않은 이전 갈림길이 열려 있던 게임 로직 오류를 발견했다. 완료한 지점의 다음 행 연결점만 열고, 아직 끝내지 않은 방문에서 다른 갈림길로 이동하는 것을 막았다. 회귀 테스트 2개 추가.
- 비교용 후속 화면은 `screens/dungeon-entry.png`, `screens/battle-guide.png`, `screens/battle-reward.png`. Android 전체 TalkBack/전체 전투 화면의 극단 글자 크기/5개 구역 밸런스 검증까지 완료했다는 뜻은 아니다.
